<UserControl x:Class="TheManager_GUI.controls.ControlNewsItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TheManager_GUI.controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" Style="{StaticResource textSecondary}" Text="{Binding Title}" Margin="0, 0, 0, 10"/>
		<TextBlock Grid.Row="1" Style="{StaticResource textPlain}" TextWrapping="Wrap" Text="{Binding NewsContent}"/>
		<Rectangle Grid.Row="2" VerticalAlignment="Stretch" Width="100" Margin="30" Stroke="{StaticResource colorTitle2}" />
	</Grid>
</UserControl>
